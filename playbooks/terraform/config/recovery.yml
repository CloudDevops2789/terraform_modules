---
terraform_stack: recovery

terraform_var_files:
  - common-tags.tfvars
  - recovery.tfvars

terraform_environment_variable_allowlist: []

terraform_runtime_variable_allowlist:
  - demo_ec2_enabled

terraform_dependencies:
  persistent:
    - standard_backup_vault_name
    - air_gapped_backup_vault_arn

  platform:
    - platform_contract

terraform_expected_destroy_confirmation: "DESTROY RECOVERY"
