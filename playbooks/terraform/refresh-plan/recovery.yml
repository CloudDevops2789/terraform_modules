---
- name: Refresh-plan recovery Terraform state
  import_playbook: ../common/deploy.yml
  vars:
    terraform_stack: recovery
    terraform_operation: refresh-plan
    terraform_repository_root: "{{ playbook_dir }}/../../.."
