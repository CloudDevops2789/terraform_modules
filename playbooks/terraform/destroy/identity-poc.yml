---
- name: Destroy identity-poc Terraform stack
  import_playbook: ../common/destroy.yml
  vars:
    terraform_stack: identity-poc
    terraform_operation: destroy
    terraform_repository_root: "{{ playbook_dir }}/../../.."
