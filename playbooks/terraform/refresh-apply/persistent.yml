---
- name: Refresh-apply persistent Terraform state
  import_playbook: ../common/deploy.yml
  vars:
    terraform_stack: persistent
    terraform_operation: refresh-apply
    terraform_repository_root: "{{ playbook_dir }}/../../.."
